apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mernspace-catalog-cert
spec:
  secretName: catalog-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: api-pizza.adityavyas.com
  dnsNames:
  - api-pizza.adityavyas.com
