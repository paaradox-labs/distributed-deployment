apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mernspace-notification-cert
spec:
  secretName: api-pizza-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: api-pizza.adityavyas.com
  dnsNames:
  - api-pizza.adityavyas.com
